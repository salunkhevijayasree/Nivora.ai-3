# Render Deployment Blueprint for NIVORA AI Backend
services:
  - type: web
    name: nivora-ai-backend
    env: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
